server:
  port: 8080

spring:

  application:
    name: KnowledgeSphere

  datasource:
    url: jdbc:postgresql://ep-delicate-pine-aix2ag7p-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require&tcpKeepAlive=true&connectTimeout=30&socketTimeout=300
    username: neondb_owner
    password: npg_ptkb04hHLSfl
    driver-class-name: org.postgresql.Driver

    hikari:
          maximum-pool-size: 5
          minimum-idle: 1
          connection-timeout: 60000
          validation-timeout: 5000
          max-lifetime: 1800000
          keepalive-time: 300000
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  ai:
    openai:
      api-key: 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
      base-url: https://openrouter.ai/api/v1

      chat:
        options:
          model:  nvidia/nemotron-3-ultra-550b-a55b:free
          temperature: 0
      embedding:
        enabled: false

    ollama:
      base-url: http://localhost:11434

      embedding:
        options:
          model: mxbai-embed-large:latest

    vectorstore:
      pgvector:
        initialize-schema: true
        table-name: vector_store

springdoc:
  swagger-ui:
    path: /swagger-ui.html

jwt:
  secret: D9f7zKqP3x1M8o1L4p3Fj+0yZkU8rVrNwG5sXq2TzR0=
  expiration: 86400000



logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.hibernate.orm.schema: DEBUG
    org.springframework.ai: DEBUG
    org.springframework.security: INFO

app:
  storage:
    location: uploads/documents

docling:
  base-url: http://localhost:8001

ppstructure:
  base-url: http://localhost:8000

vlm:
  base-url: https://quench-frustrate-rockiness.ngrok-free.dev